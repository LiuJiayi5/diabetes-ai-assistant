spring:
  datasource:
    url: jdbc:h2:mem:diabetes_assistant_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: diabetes_assistant_test_secret_123456
  expire-seconds: 86400
